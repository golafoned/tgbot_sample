{
  "name": "telegram-demo-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "express": "^4.13.3",
    "node-telegram-bot-api": "^0.15.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node index.js",
    "test": "echo \"VERCEL_ENV: $VERCEL_ENV\"

if [[ \"$VERCEL_ENV\" == \"production\" ]] ; then
  # Proceed with the build
  echo \"✅ - Build can proceed\"
  exit 1;

else
  # Don't build
  echo \"🛑 - Build cancelled\"
  exit 0;
fi"
  },
  "author": "",
  "license": "ISC"
}
